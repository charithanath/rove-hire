// Public candidate form — implementation in Milestone 4
export default function ApplyPage({ params }: { params: { token: string } }) {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-muted">Candidate application form — coming soon.</p>
    </div>
  );
}
